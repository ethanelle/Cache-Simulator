#include <iostream>
#include <fstream>
#include <vector>
#include <string>

struct full_Entry {
	unsigned int valid;
	unsigned int tag;
};

void full_assoc(std::vector<std::string> list, std::fstream &outFile)
{
	int total, hit;
	unsigned int 
}
